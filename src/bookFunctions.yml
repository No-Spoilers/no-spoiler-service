BookFunctions:

  postBook:
    handler: src/handlers/book/postBook.handler
    events:
      - http:
          method: POST
          path: /book

  deleteBook:
    handler: src/handlers/book/deleteBook.handler
    events:
      - http:
          method: DELETE
          path: /book/{bookId}
